# AutoCAD 双向实时桥接 · 使用指南

本指南说明如何通过 `bridge.py` + `cad.py` 接入 AutoCAD 2022，实现**双向实时**绘图与读取。

---

## 1. 这是什么

一个**常驻桥接进程**，通过 COM 自动化长连接本机 AutoCAD，让外部程序（如 Claude Code / 脚本）能：

- **下行**（外部 → CAD）：画图、查询、改系统变量、保存，实时拿到返回结果；
- **上行**（CAD → 外部）：读回实体、几何、图层，以及**变更检测**（感知图形何时被改动）。

图形状态全程驻留，AutoCAD 不重启、不掉线。

---

## 2. 架构与原理

```
[调用方 / 脚本 / CLI] ──TCP 127.0.0.1（长连接复用）──► [桥接进程]
                                                        │  win32com COM 长连接
                                                        │  (AutoCAD.Application.*)
                                                        ▼
                                                    [AutoCAD]
                                                指令 ↓   实体/状态 ↑
```

- `cadbridge`（或 `cadbridge.exe`）：**一个入口两种角色**。前台跑命令时是客户端；
  被 `daemon.ensure_running()` 以 `--serve` 拉起时是常驻桥接。
- **桥接进程是 AutoCAD 的唯一 COM 连接者**。多开桥接 = 多路 COM 连接 = 必然把
  AutoCAD 卡进 `RPC_E_CALL_REJECTED`，所以用命名互斥体做了单实例保护。
- `state.json`：桥接启动时写入端口 / PID / 协议版本 / 模式，供客户端定位。
- 客户端复用一条长连接发所有请求（建模脚本动辄上百条指令，每请求一条连接
  会耗尽本机临时端口）。

**“实时”的实现**：桥接进程和 AutoCAD 之间的通信是即时同步的；调用方每次交互都直接读到 AutoCAD 当前状态，所以交互上等同于实时。上行变更用 `changes` 轮询（见 §6）。

---

## 3. 环境与依赖

| 项 | 值 |
|----|----|
| 操作系统 | Windows |
| AutoCAD | 2018–2026（本机实测 2022，`ACAD-5101:804`，简体中文，R24.1） |
| COM ProgID | **自动探测** `AutoCAD.Application.*`，优先用最新版；不硬编码 |
| Python（源码运行） | 3.12 + pywin32 |
| Python（打包运行） | 不需要，exe 自带 |

**AutoCAD 是外部依赖，本工具不捆绑、不附带**，需要用户自备许可。软件只做两件事：
探测本机已装的版本，以及（需要时）把 `acad.exe` 拉起来。定位顺序是三级降级：

1. 已在运行的实例（`GetActiveObject`）—— 最快，也不打扰用户；
2. 注册表里已注册的 ProgID，按版本从新到旧取第一个；
3. 从 `LocalServer32` 反查 `acad.exe` 路径，用 `/Automation` 主动拉起。

三级全失败才会报错，并且报错信息会直接说清楚缺的是什么。

---

## 4. 快速开始

```bash
# 1) 自检：环境缺什么一步到位（会自动拉起桥接和 AutoCAD）
cadbridge doctor
cadbridge doctor --deep      # 再加一步：真画一条线做端到端验证

# 2) 直接用，不用先手动启动任何东西
cadbridge line 0,0 100,100
cadbridge circle 50,50 25
cadbridge text "中文标注" --at 10,10 --height 8
cadbridge count
```

> **启动是自动的**：第一次调用会拉起后台桥接，桥接再拉起 AutoCAD。
> 首次冷启动（要等 AutoCAD 起来）大约 10~60 秒，之后每条命令都是毫秒级。

源码运行等价于 `python cadbridge.py <命令>`；打包后就是 `cadbridge.exe <命令>`。

> 提示：可从任意目录用绝对路径调用，例如 `D:/Python312/python.exe D:/P/cad/cadbridge.py info`。

---

## 5. 命令参考

所有命令都是一行 JSON，`cmd` 字段区分动作。返回也是一行 JSON，形如 `{"ok":true, ...}` 或 `{"ok":false,"error":"..."}`。

### 5.1 连接 / 状态

| 命令 | 说明 | 示例 |
|------|------|------|
| `ping` | 探活 | `{"cmd":"ping"}` → `{"ok":true,"pong":true}` |
| `info` | 版本 / 文档 / 实体数 / 可见性 | `{"cmd":"info"}` |
| `count` | 模型空间实体总数 | `count`（裸词简写） |

### 5.2 绘图

| 命令 | 参数 | 说明 |
|------|------|------|
| `add_line` | `start:[x,y,z]`, `end:[x,y,z]` | 直线 |
| `add_circle` | `center:[x,y,z]`, `radius:r` | 圆 |
| `add_text` | `text:str`, `insert:[x,y,z]`, `height:h` | 单行文字 |
| `add_polyline` | `points:[[x,y],...]`, `closed:bool` | 多段线（`closed:true` 闭合，三维建模要用） |
| `add_point` | `point:[x,y,z]` | 点 |
| 命令 | 参数 | 说明 |
|------|------|------|
| `layer` | `name:str`, `color:1-255`, `linetype:str`, `active:bool` | 建/改图层并可选设为当前。线型没加载会自动从 `acad.lin` 载入 |
| `linetype` | `name:str`, `file:str` | 从线型文件加载线型（`DASHED` / `DASHDOT` 等） |
| `hatch` | `boundary:H`, `pattern:str`, `scale:num`, `angle:num`, `color:1-255`, `layer:str` | 按闭合边界做图案填充。图案不在 `.pat` 里会明确报错，便于退到备用图案 |
| `add_dim` | `mode:"rotated"/"aligned"`, `p1:[..]`, `p2:[..]`, `loc:[..]`, `angle:弧度`, `color:1-255`, `text_height:h` | **尺寸标注**。`p1`/`p2` 是被标注的两点，`loc` 定位尺寸线。`rotated` 配 `angle`（0=水平、π/2=垂直）；`aligned` 沿两点连线 |
| `dimstyle` | `color:1-255` | 改当前标注样式。`color` 走 `DIMCLRD`，即**只把尺寸线染绿而文字不变** |
| `textstyle` | `name:str`, `font:str` | 建/改文字样式并设为当前。中文用 `font:"SimHei"`（TrueType 传**字体名**，不是 `simhei.ttf`） |
| `open` | `path:str` | 打开 DWG 并切为当前文档 |
| `new` | — | 新建图纸并切为当前文档 |
| `docs` / `activate` | `name:str` | 列出已打开图纸 / 切换到指定图纸 |

```bash
python cad.py '{"cmd":"add_line","start":[0,0,0],"end":[100,100,0]}'
python cad.py '{"cmd":"add_circle","center":[50,50,0],"radius":25}'
python cad.py '{"cmd":"add_text","text":"中文标注","insert":[10,10,0],"height":5}'
python cad.py '{"cmd":"add_polyline","points":[[0,0],[100,0],[100,80],[0,80],[0,0]]}'
python cad.py '{"cmd":"textstyle","name":"S23","font":"SimHei"}'   # 之后 add_text 即可正常显示中文

# 画一个 200×120 的长方形，并在下方/右侧各标一个绿色尺寸
python cad.py '{"cmd":"add_polyline","closed":true,"points":[[0,0],[200,0],[200,120],[0,120]]}'
python cad.py '{"cmd":"add_dim","mode":"rotated","p1":[0,0],"p2":[200,0],"loc":[100,-28],"angle":0,"color":3}'
python cad.py '{"cmd":"add_dim","mode":"rotated","p1":[200,0],"p2":[200,120],"loc":[228,60],"angle":1.5707963,"color":3}'
```

> **颜色**用 AutoCAD 颜色索引（ACI）：1 红、2 黄、**3 绿**、4 青、5 蓝、6 洋红、7 白/黑。
> 尺寸也是普通图元，`color` 会把尺寸线、箭头、文字一起染色；只想染尺寸线就用 `dimstyle` 的 `DIMCLRD`。
>
> 尺寸走 COM 的 `AddDimRotated` / `AddDimAligned`，**不是** `sendcommand` 敲 DIMLINEAR ——
> 后者会停在命令行等输入，把 AutoCAD 卡成忙态（见 §8 约束 7）。

完整可运行示例见 [`draw_rect_dim.py`](draw_rect_dim.py)（长方形 + 两个绿色尺寸）。

返回示例：`{"ok":true,"handle":"2A2","object":"AcDbLine"}`

### 5.3 查询 / 读回

| 命令 | 参数 | 说明 |
|------|------|------|
| `entities` | `limit:n`（默认 500） | 枚举实体，含几何（线→Start/End、圆→Center/Radius、文字→Text/Insert、多段线→Coords） |
| `selection` | — | 当前拾取集（Pickfirst）实体 |
| `getvar` | `name:str` | 读系统变量 |
| `setvar` | `name:str`, `value` | 写系统变量 |

```bash
python cad.py '{"cmd":"entities","limit":100}'
python cad.py '{"cmd":"getvar","name":"CLAYER"}'
```

### 5.4 变更检测（上行实时）

| 命令 | 说明 |
|------|------|
| `changes` | 返回 `count` + `sig`（MD5 签名）+ `handles` 列表 |

两次调用之间 `sig` 变化即表示图形被改动（新增/删除/修改）。

```bash
python cad.py changes
# {"ok":true,"count":5,"sig":"41ed5450cbaf","handles":["2A2","2A3",...]}
```

### 5.5 控制

| 命令 | 参数 | 说明 |
|------|------|------|
| `sendcommand` | `text:str` | 发原始命令串（异步、无返回） |
| `zoom` | `mode:"extents"/"all"` / `mode:"center"`+`center:[x,y,z]`+`height:h` | 缩放 |
| `save` | `path:str` | 另存为 DWG |
| `shutdown` | — | 退出桥接（**CAD 保留运行**） |
| `quit` | — | 关闭 AutoCAD 并退出桥接 |

### 5.6 三维实体建模

拉伸方向恒为 +Z，所以竖直件靠旋转摆正。建面域的曲线必须是**闭合**的 2D 多段线
（`add_polyline` 传 `closed:true`）；面域和源曲线会被拉伸消耗掉。

| 命令 | 参数 | 说明 |
|------|------|------|
| `add_region` | `handles:[...]` | 闭合曲线 → 面域 |
| `extrude` | `region:H`, `height:h`, `taper:t` | 面域 → 拉伸实体（**沿 +Z**） |
| `add_box` | `origin:[x,y,z]`, `l`, `w`, `h` | 长方体（l 沿 X、w 沿 Y、h 沿 Z） |
| `add_cylinder` | `center:[x,y,z]`, `radius`, `height` | 圆柱（底面中心起，沿 +Z） |
| `boolean` | `op:"union"/"subtract"/"intersect"`, `target:H`, `tools:[H,...]` | 布尔运算 |
| `move` | `handle:H`, `from:[...]`, `to:[...]` | 平移 |
| `rotate3d` | `handle:H`, `p1:[...]`, `p2:[...]`, `angle:弧度` | 绕 p1→p2 轴旋转（右手定则） |
| `setprop` | `handle:H`, `color:1-255`, `layer:"..."` | 改颜色 / 图层 |
| `erase` | `handles:[H,...]` | 删除实体 |
| `bbox` | `handle:H` | 读包围盒 |
| `view3d` | `dir:[x,y,z]` | 设三维视点（相机相对目标的方位） |
| `visualstyle` | `name:"S"` | 切视觉样式，见约束 7 |

```bash
python cad.py '{"cmd":"add_polyline","points":[[0,0],[78,0],[78,163],[0,163],[0,0]],"closed":true}'
python cad.py '{"cmd":"add_region","handles":["2A2"]}'
python cad.py '{"cmd":"extrude","region":"2A3","height":8.9}'
```

> **三维要点**：AutoCAD 三维视图里世界 **+Z 是屏幕上方**，想让零件立着它就得沿 +Z 长；
> 而拉伸只能沿 +Z，所以要「在 XZ 平面画轮廓 → 拉伸 → `rotate3d` 绕 X 轴 -90° 立起来」。
> 绕 X 轴 -90° 的映射是 `(x,y,z) -> (x, z, -y)`，平移量要按 `z0+z1` 反算。

### 5.7 图形控制台

```bash
cadbridge gui            # 源码运行
cadbridge-gui.exe        # 打包后双击
```

四个标签页：**状态**（一键自检，按级别着色）、**绘图**（直线/圆/文字/矩形，填参数就画）、
**控制台**（内嵌命令行，短命令或完整 JSON）、**日志**（桥接日志尾部 + 打开数据目录）。
顶部状态条显示桥接 / AutoCAD 版本 / 实体数 / 端口，带自动刷新开关。

**它只是 CLI 的一层皮**：所有实际操作仍走 `protocol.request` → 桥接 → AutoCAD，
GUI 自己绝不碰 COM（那会变成第二路连接，见 §8 约束 1）。

两条界面实现上的约定：

- **耗时操作一律在后台线程**，结果经队列回主线程用 `after()` 消费。tkinter 只能在
  主线程碰控件，直接在工作线程里 `configure()` 会随机崩。
- **自动刷新是「安静」的**：不占用忙碌标志、不禁用按钮、不显示忙碌提示。
  否则每几秒就把界面冻一次，看着像坏了。

### 5.8 工程图标准（基坑支护施工图）

**标准不是「描述」，是那张演示图本身。** `eg/演示.dwg` 里带着这套标准的
全部载体：`CSSDI-A3` 图框块（内含院徽图签栏）、23 个图层、5 个文字样式、
2 个标注样式。所以做法是**拿它当模板**，而不是自己另画一套 —— 后者做出来
永远只是「像」，不是。

```bash
python draw_section.py CD段 --template    # 复制演示图 → 清空 → 在新图里画
python draw_section.py AB段 --template    # 换一组参数（演示同源的桩撑/锚）
```

**自动绘图**：参数可以直接来自计算书，不必手写。

```bash
python draw_section.py --rtf eg/桩悬臂.rtf   # 一份计算书 → 一张图
python draw_section.py --dir 计算书目录/      # 一批计算书 → 一册图（并排排开）
python draw_section.py CD段 --template       # 用内置参数
```

`cadkit/calcbook.py` 负责解析**天汉基坑设计软件**导出的 RTF 计算书。
原 VB 读这些参数用的是「段落序号 + `InStr` 截取」（`Paragraphs(nPara)`），
计算书多一行少一行就全错位；这里改成按标题切段再逐行解析，稳得多。

解析出的字典结构与 `draw_section.py` 的 `CASES` 一致，所以下游不用区分来源。

> **两个易错点**，都写了回归测试：
> ① 计算书里同一标高常给「绝对值(相对值)」两个数，**顺序还不固定** ——
> `基底标高 17.000m(-6.000m)` 是绝对值在前，`桩顶标高 -2.500m(20.5m)`
> 是绝对值在后。取错图上就全歪。判据是取与「结构正负零标高」量级接近的那个。
> ② 工况表的行首是「工况1」不是数字，按「数字开头」过滤会把整张表滤掉。

`cadkit/standard.py` 记的是**从演示图与 `eg/Drawing Module.vb` 里量出来的**
标准值，供补建图层、查土层填充图案用：

| 内容 | 来源 |
|------|------|
| 21 个标准图层名 / 线型 / 颜色 | `PileSupport()` 的 `Layers.Add` 段 |
| 土层名 → 填充图案 / 颜色 | `soilpattern()` |
| 坐标系（y=0 地面线，向下为负，单位 mm） | 各绘图段 |
| 图框块几何（`CSSDI-A3` @ (-1000,-11000)，内部嵌 420×297 的 A3 块按 100 倍放大） | 实测演示图 |
| 文字样式 `PM-TEXT`，字高 400 | 实测演示图（566 个文字全用它） |
| 图签栏各格落字位置 | 实测演示图（块里没有属性，格子是空的） |
| 相邻两张图的间距 52000 | 实测演示图（11 个图框的排布） |

**支护类型支持情况** —— 绘图程序按类型分发，代码在 `cadkit/sections/`：

| 类别 | 类型 | 说明 |
|------|------|------|
| 主体 | `pile` | 桩锚 / 悬臂桩。单级放坡→平台→竖壁 |
| 主体 | `slope` | 放坡土钉。**多级台阶轮廓**（2309 行的 `slopeSupport`）|
| 主体 | `double` | 双排桩：前后排 + 冠梁 + 连梁 + 桩间土 |
| 附加 | `passive` | 被动区加固：坑底以下按台阶布加固桩 |
| 附加 | `bracing` | 内支撑 + 格构柱 + 立柱桩 |
| 附加 | `reinforce` | 桩身配筋大样（圆形/矩形截面）|
| 附加 | `notes` | 设计施工说明文字块 |

**主体**决定开挖轮廓、出一张独立图纸；**附加**叠加在主体剖面上。
参数里 `附加: ["passive", "notes"]` 指定叠哪些 —— 任一主体都能配任一组附加。
原 VB 把这些揉在同一个过程里，拆开之后组合自由得多。

`slope` 类型覆盖了 `Ctx.profile_x` / `profile_breaks` / `profile_break_inner`
三个钩子，把开挖轮廓换成多级台阶，土层多边形会自动跟着台阶走 ——
所以两种主体共用同一套土层绘制代码。

> 新增一类主体时，最容易犯的错是**几何自相矛盾**（多边形自交、标高顺序反了）。
> `tests/test_drawing.py` 里的 `TestAllTypes` 会把每种内置参数都跑一遍几何校验，
> 不需要 AutoCAD 就能挡住这类错。

**已知限制**：土层的 `CQ*` 填充图案是本项目**自带**的自定义图案
（`CQ杂填土`、`CQ粉质粘土`…），标准 `acad.pat` 里没有；本机也没有
`Tssdeng.shx`（`PM-TEXT` 的原字体）。两者都做了自动降级并在输出里标明，
要完全一致需把这两样补到 AutoCAD 的字体/图案目录。

---

## 6. 双向实时机制详解

### 6.1 下行（外部 → CAD）

直接走 COM 对象模型，**同步返回**：`AddLine`、`AddCircle`、`AddText` 等都有返回值（句柄、对象名）。任意命令都能即时拿到结果。

### 6.2 上行（CAD → 外部）

两种方式：

1. **按需读回**：`entities` / `count` / `selection` / `getvar` 随时读取当前图形状态。
2. **变更检测**：连续两次 `changes`，对比 `sig` 即可知道图形是否被改过；需要细节时再 `entities` 拉全量。

> 为什么不用 COM 事件（`ObjectAdded`/`SelectionChanged` 等）？见 §8 约束 2。

---

## 7. 完整示例：画一个带标注的矩形

```bash
# 四条边
python cad.py '{"cmd":"add_line","start":[0,0,0],"end":[200,0,0]}'
python cad.py '{"cmd":"add_line","start":[200,0,0],"end":[200,100,0]}'
python cad.py '{"cmd":"add_line","start":[200,100,0],"end":[0,100,0]}'
python cad.py '{"cmd":"add_line","start":[0,100,0],"end":[0,0,0]}'
# 中心圆 + 标注
python cad.py '{"cmd":"add_circle","center":[100,50,0],"radius":30}'
python cad.py '{"cmd":"add_text","text":"200×100","insert":[90,45,0],"height":10}'
# 缩放到全图并读回
python cad.py '{"cmd":"zoom","mode":"extents"}'
python cad.py changes
```

---

## 8. 关键约束（务必遵守）

1. **只走 `cad.py`（桥接）**。绝不要另开进程用 `win32com.GetActiveObject(...)` 直连 AutoCAD —— 第二路 COM 连接会把 AutoCAD 的 COM 消息过滤器卡进 `RPC_E_CALL_REJECTED` 状态，此后所有调用被拒，只能重启 AutoCAD。
2. **事件订阅默认关闭**（`bridge.py` 里 `ENABLE_EVENTS=False`）。本版本 AutoCAD（`24.1s LMS Tech`）在 `AddLine` 等调用期间触发 `ObjectAdded`/`ObjectModified` 事件回调会**重入并抛 `RPC_E_SERVERFAULT`，污染数据库**（实体加不进去、出现幽灵实体）。上行实时因此改用 `changes` 轮询。
3. **中文**：`cad.py` 已内置 argv 重解码，中文文字可直接传，无需加 `PYTHONUTF8`。
4. **遇 `RPC_E_CALL_REJECTED`**：桥接已内置自动重试（8 次 × 0.6s）。若仍持续，重启 AutoCAD 即可。
5. **不要在 `sendcommand` 里发 `-STYLE`**：该命令会停在命令行等输入，AutoCAD 随即进入忙态、所有 COM 调用被 `RPC_E_CALL_REJECTED` 顶回来（只能对 AutoCAD 窗口发 ESC 解除）。建文字样式请用 `textstyle` 指令。
6. **中文文字**：默认 `Standard` 样式是 SHX 字体，中文会显示成 `?`。画中文前先 `textstyle` 建一个 TrueType 样式。
7. **`VSCURRENT` 之类的选项提示，关键字要用「本地缩写」**：中文版 `VSCURRENT` 的选项是 `概念(C)`，命令串里必须写 `C`——写中文全称 `概念` 或加下划线 `_C` 都不认（下划线只翻译命令名，不翻译选项缩写）。不认就会**停在命令行反复等输入**，AutoCAD 随即进入忙态、所有 COM 调用被 `RPC_E_CALL_REJECTED` 顶回。
8. **`boolean` 会把目标实体的颜色重置回 ByLayer(256)**：设颜色必须放在布尔运算**之后**。
9. **三维实体的 `GeometricExtents` / `Color` 在 pywin32 早绑定包装里不存在**：桥接已改用 `dynamic.Dispatch` 迟绑定。
10. **打包后 `sys.argv` 的起点不一样**：源码是 `python.exe 脚本.py 参数…`（第 1 项是脚本名），
    exe 是 `cadbridge.exe 参数…`（**没有**脚本名那一项）。窄字符版 argv 修复逻辑里写死从第 1 项取，
    会让打包版把第一个参数当成脚本名丢掉 —— 现场表现是**所有子命令都退化成打印帮助**。
11. **中文输出必须显式钉成 UTF-8**：管道 / 重定向时 Python 按系统 ANSI 代码页（简体中文是 cp936）编码，
    下游按 UTF-8 读就是乱码。源码模式常被 `PYTHONIOENCODING=utf-8` 掩盖，打包后没有这个环境变量就现原形。
    桥接启动时统一 `reconfigure(encoding="utf-8")`。
12. **onefile 打包的桥接不能用父进程的解包目录**：PyInstaller 会把解包目录通过 `_MEIPASS2`
    传给子进程，子进程看到就不再自己解包。而桥接是**长期驻留**的 —— 父进程退出时去删那个临时目录，
    删成功就把正在运行的桥接弄崩了。派生 daemon 前必须清掉 `_MEI*` / `_PYI_*` 环境变量。
13. **客户端必须复用长连接**：建模脚本动辄发上百条指令，每请求一条连接会堆满 TIME_WAIT。
    实测把本机临时端口（16384 个）耗光后，`connect` 直接报 `WSAEADDRINUSE(10048)`。
    现在协议是「一条连接上按行循环收发多条指令」。
14. **单实例保护用命名互斥体，不要用文件锁**：早先的文件锁版本在锁定第 0 字节后又 `truncate()`
    截断文件，等于把刚锁上的区域截没了，锁形同虚设（现场表现就是 `bridge.lock` 是空的）。
    互斥体是内核对象，进程无论怎么死都会自动释放。互斥体名由数据目录派生，`CADBRIDGE_HOME` 隔离才有效。
15. **空连接是存活探测，不能当成指令处理**：客户端探活时会开一条连接立刻关闭。
    若把这个空请求当成 `{"cmd":""}` 走完整流程，会触发 AutoCAD 健康检查 ——
    结果就是**一次只读的 `status` 查询把 AutoCAD 给重启了**。
16. **`LocalServer32` 的值不能按空格切**：实测本机是 `D:\Programs\cad22\AutoCAD 2022\acad.exe /Automation`，
    不加引号且路径含空格。按空格取第一段会得到不存在的 `D:\Programs\cad22\AutoCAD`。
    正确做法是按 `.exe` 定位并校验文件存在。
17. **回环端口必须做鉴权**：`sendcommand` 是原样执行的 AutoCAD 命令串，
    无鉴权时本机任意进程连上端口就能清空图纸。见 §10。
18. **`.ps1` 要带 BOM，`.cmd` 不能带**：Windows PowerShell 5.1 对无 BOM 的 UTF-8
    按 ANSI 解码，中文变乱码字节后直接报 `UnexpectedToken` 解析失败；
    批处理反过来，带 BOM 会让 cmd.exe 误读首行。二者要求正好相反。
    另外**别在 .cmd 里 `chcp`**：中途切代码页会让 cmd.exe 读错后续行。
    需要复杂逻辑就让 .cmd 转发给 .ps1，用 `$PSScriptRoot` 取路径，
    不要用 `%~dp0` 拼多层转义的 PowerShell 单行命令。
19. **验证 PATH 改动不能靠 `[Environment]::GetEnvironmentVariable`**：该 API 在同一进程内
    有环境缓存，子进程改了注册表它也可能读到旧值，会得出「改了但读不到」的假象。
    直接读 `HKCU:\Environment` 才是真实值。另外 `$env:TEMP` 常给 8.3 短路径
    （`ADMINI~1`），而 `$PSScriptRoot` 给长路径，字符串比对时要先归一化。
20. **长连接 + 单线程 accept = 一条空闲连接堵死所有客户端**：客户端为了不耗尽临时端口
    会复用长连接（约束 13），而长连接大部分时间空闲。若服务端还是「accept 一条 →
    处理到它关闭为止」，就会阻塞在那条空闲连接上 —— 新连接的 SYN 堆在 backlog 里，
    堆满就被内核丢掉。现场表现极易误诊：**桥接明明在监听，`status` 却报「桥接未运行」**，
    绘图请求则一路超时。服务端已改成 `select` 多路复用。
    刻意**不用「每连接一个线程」**：AutoCAD 的 COM 对象是在主线程创建的，
    跨线程调用需要 marshaling，风险比收益大。
21. **探活超时要短**：`is_alive` 走的 `create_connection` 在回环上探一个没监听的端口
    不会立刻被拒，而是一路等到超时。这函数在自动刷新里是热路径，超时给 1.5s
    就意味着界面三成时间在「忙碌」。现在给 0.5s。
22. **后台任务不能和用户操作共用「忙碌」标志**：否则自动刷新每几秒就把按钮禁一次，
    界面看着像坏了；而如果只是 `if busy: return` 静默返回，用户点击就被无声吞掉
    （「按了没反应」）。正确做法是——用户操作之间互斥并**置灰按钮**给出反馈，
    后台刷新则完全不影响界面。
23. **`DETACHED_PROCESS` 拦不住窗口子系统父进程派生的控制台窗口**：从 GUI exe
    （无控制台）里拉起控制台程序时，Windows 仍会新建一个黑窗口挂在旁边。
    CLI 场景看不出来（父进程本来就有控制台），只有打包成 GUI exe 后才暴露。
    要用 `CREATE_NO_WINDOW`。
24. **GUI exe 派生桥接必须指向 CLI exe**：`sys.executable` 在 GUI 进程里是
    `cadbridge-gui.exe`，`--serve` 传过去会被 GUI 入口忽略（它不解析参数、直接开窗口），
    结果是桥接起不来、反而弹出一堆窗口。`daemon._spawn_cmd()` 现在优先找同目录的
    `cadbridge.exe`；同时 `cadbridge_gui.py` 也处理 `--serve` 作为兜底。
25. **`/SUPPRESSMSGBOXES` 不会抑制 `[Code]` 里自己调的 `MsgBox`**：静默安装时那些框
    没人点，安装程序会一直挂着 —— 而且**文件、PATH、快捷方式都已写好，只是进程不退出**，
    极具迷惑性。所有自己调的 `MsgBox` 前面都要 `if WizardSilent then exit;`。
26. **Inno Setup 不自带简体中文界面**：`ChineseSimplified.isl` 是社区翻译，需另外获取，
    放进 Inno Setup 的 `Languages` 目录或本项目的 `installer/languages/` 均可。
    `.iss` 里用 `#if` 条件加载，找不到就退回英文界面（功能不受影响）。
    注意 `[CustomMessages]` 里的中文条目必须和 `[Languages]` 同进同退，
    否则报 `Unknown language name "chinese"`。
27. **winget 装的 Inno Setup 在 `%LOCALAPPDATA%\Programs` 下**（按用户安装），
    只查 `Program Files` 会找不到 `ISCC.exe`。`build.py` 现在先读注册表里的
    `InstallLocation`，再退化为猜路径。
28. **早绑定包装缺属性这件事，已经踩到第四次**：`IAcad3DSolid` 的 `Color`/`GeometricExtents`、
    尺寸的 `Color`、`IAcadLayer` 的 `Color`、`IAcadHatch` 的 `Color` —— 全都不在
    pywin32 的早绑定包装里。**凡是给实体设属性，一律 `dynamic.Dispatch(obj)` 迟绑定**，
    不要再试早绑定。报错形如 `'IAcadXxx' object has no attribute 'Color'`。
29. **同一个 y 上轮廓可能是多值的**：基坑放坡底（`y = -放坡总高`）处，坡脚在 1500、
    平台外沿在 2500。按深度取右边界的函数必须明确取哪一个，否则土层多边形会从坡脚
    斜拉到平台外沿，画出一条不该有的斜边。土层顶边取外沿（往下是延伸到坑壁的），
    底边若正好落在坡底则取坡脚（平台是挖掉的，不覆盖）。
30. **画中文前必须建 TrueType 文字样式**：这条和约束 6 是同一件事，但极易漏 ——
    生成脚本里如果忘了 `textstyle`，整张图的中文会静默变成 `?`，不报任何错。
    写生成器时把它放进「建图层」那一步一起做。
31. **缺字体时 AutoCAD 什么都不画**（不是显示成 `?`，是彻底空白）。
    标准图的 `PM-TEXT` 样式用的是 `Tssdeng.shx`（探索者 TSSD 字体，路径指向
    AutoCAD 2012），本机没这文件，用它写的文字**一个都不显示** ——
    而实体确实建出来了、图层也开着，排查时极容易误判成「没画上」。
    先试原字体，装了就照用，没装再换 `gbenor.shx + gbcbig.shx`（国标 SHX 两件套）。
32. **`SetFont` 对 TrueType 要字体名、对 SHX 干脆不吃**：传 `simhei.ttf` 报
    「输入无效」，要传 `SimHei`；而 `gbenor.shx` 之类走 `SetFont` 一律失败，
    得直接写 `ts.fontFile` 属性。中文字体是「西文 SHX + 大字库 SHX」两件套，
    还必须另设 `BigFontFile`，只设前一个中文照样不显示。
33. **给实体设一个不存在的图层会报「未找到主键」，而且报在建图元那一步**，
    看起来像几何数据有问题。模板来自既有图纸时，图层未必齐全
    （演示图里就缺 `桩间挂网`、`竖向隔渗帷幕`、`平面图用轴线`）。
    用之前先列一遍图层，缺的按标准表补建；已有的**不要**重建，否则会覆盖
    模板里配好的颜色/线型。
34. **`erase_all` 必须如实回报失败**：早先 `except: pass` 把删除失败全吞了，
    报「已清空」其实只删掉一部分，残留图元混进新图里，看起来就像画错了。
35. **`Documents.Open` 对已打开的文件会再开一个新文档**，不是复用。
    反复 open 同一个文件会堆出一排同名标签页，之后的操作落在哪张上完全不确定。
    打开前先按 `FullName` 查一遍、有就 `Activate`。
36. **建图元时直接带 `layer`/`color`**，别走「先创建、再按句柄 setprop」——
    后者要 `HandleToObject` 回查，偶发失败，而且失败后图元会静静留在当前层上
    （比如模板的「地下室」），很难发现。
37. **拉起 AutoCAD 前必须先用进程表确认一遍**，不能只看 `GetActiveObject`：
    AutoCAD 刚启动或正忙时会话还没注册进 ROT，探测会失败。若据此判定「没开」
    就再拉一个，结果是**一个窗口接一个窗口地冒出来** —— 而 AutoCAD 允许多实例，
    全程不报任何错，只看到窗口越来越多。现在流程是：进程在就直接等它注册，
    进程不在才启动。所以调测时也别反复 `stop`/`start` 桥接 —— 每重启一次，
    只要 AutoCAD 不在就会拉一个新的起来（实测一轮下来冒出过 8 个）。

---

## 9. 打包与分发

```bash
python build.py              # 打包 exe（图标不存在会自动生成）
python build.py --portable   # 额外产出免安装 zip
python build.py --installer  # 额外产出安装程序（需要 Inno Setup）
python build.py --all        # 三样都出
```

打包后目标机器**不需要装 Python**，只需要装 AutoCAD。

| 形态 | 产物 |
|------|------|
| 裸 exe | `dist/cadbridge.exe`（CLI，约 9 MB）+ `dist/cadbridge-gui.exe`（GUI，约 12 MB） |
| 便携包 | `dist/CadBridge-<版本>-portable.zip`（两个 exe + 文档 + 安装/卸载.cmd） |
| 安装程序 | `dist/CadBridge-<版本>-setup.exe`（Inno Setup，按用户安装免 UAC） |

分成两个 exe 而不是合一：做成一个的话，要么 GUI 用户看到多余的黑窗口，
要么 CLI 用户白背几 MB 的 tkinter。`cadbridge.spec` 里跑了两个 Analysis，
CLI 那份显式排除 `tkinter`。

| 文件 | 作用 |
|------|------|
| `cadbridge.py` / `cadbridge_gui.py` | 两个入口模块 |
| `cadbridge.spec` | PyInstaller 配置（两个 exe） |
| `version_info.txt` | exe 版本资源 |
| `assets/make_icon.py` | 生成 `assets/cadbridge.ico` |
| `installer/cadbridge.iss` | Inno Setup 脚本 |

**两种运行模式下的路径规则**（`cadkit/paths.py` 统一处理）：

| 用途 | 位置 | 说明 |
|------|------|------|
| 安装目录 | exe 所在目录 | 只读资源；源码模式下是项目根 |
| 数据目录 | `%LOCALAPPDATA%\CadBridge\` | state / log / config；**打包后不能写在 exe 旁边** |

> 冻结后 `__file__` 指向 PyInstaller 的临时解包目录，重启即消失。
> 所以运行期数据一律走数据目录，可用 `CADBRIDGE_HOME` 覆盖（多实例隔离、测试用）。

**安装程序的两处要点**：卸载前必须先跑 `cadbridge.exe stop`（`[UninstallRun]`），
否则正在运行的桥接占着 exe，文件删不掉；PATH 用用户级（`HKCU\Environment`），
不污染系统 PATH 也就不需要管理员权限。

**编译安装程序需要 Inno Setup 6**：

```bash
winget install JRSoftware.InnoSetup      # 按用户安装，不需要管理员
python build.py --installer
```

缺了不会导致构建失败，只会跳过并提示。想要中文向导界面，另需把
`ChineseSimplified.isl` 放进 Inno Setup 的 `Languages` 目录（或本项目
`installer/languages/`）—— 该文件不是 Inno Setup 自带的。没有就是英文界面，
功能不受影响。

**打包产物已实测**（静默安装 → 验证 → 静默卸载）：

| 检查项 | 结果 |
|--------|------|
| 文件 / 开始菜单 / 卸载器 | 均正确写入 |
| 用户 PATH | 安装后加一项，卸载后精确还原（条目数一致） |
| 注册表卸载项 | 卸载后无残留 |
| 装好的 exe | `cadbridge.exe --version` 正常 |

**依赖注意事项**：`win32com` 家族大量用运行时动态导入，静态分析追不到，
`cadbridge.spec` 里显式列了 `hiddenimports`；UPX 压缩不开（对 COM 进程注入敏感）。

## 10. 安全模型

桥接监听回环地址（127.0.0.1），**并且要求接入 token**：

- 桥接每次启动用 `secrets.token_hex(32)` 生成新 token，写进 `state.json`；
- 客户端从 `state.json` 读取并自动放进每条指令的 `_token` 字段；
- 服务端用 `hmac.compare_digest` 常数时间比对，不通过就拒绝（并记进日志）。

**为什么必须有**：`sendcommand` 是原样执行的 AutoCAD 命令串。没有这道校验，
本机任意进程只要连上端口就能发 `_ERASE _ALL` 把图纸清空，或者执行任意命令。

**防护边界**：`state.json` 在 `%LOCALAPPDATA%` 下，按用户 ACL 隔离，所以**别的用户**
读不到、连不上。同一用户下的进程仍能读到 token —— 要防住这个量级需要 OS 级隔离
（具名管道 + 安全描述符），目前未做。

## 11. 故障排查

| 症状 | 原因 | 处理 |
|------|------|------|
| `RPC_E_CALL_REJECTED`（-2147418111） | 开了第二路 COM 连接 / AutoCAD 忙 | 桥接会自动重试；持续失败看是否多开了桥接 |
| `RPC 服务器不可用`（-2147023174） | AutoCAD 被关掉或崩了，桥接握着僵尸引用 | 桥接会自动重连（必要时重新拉起 AutoCAD），无需手工干预 |
| `鉴权失败：token 不匹配` | 工具升级过，老桥接还在跑 | `cadbridge stop`，下次调用会自动按新协议拉起 |
| 桥接在监听，但 `status` 说「未运行」 | 空闲长连接堵住了 accept 循环 | 见 §8 约束 20；已改为 select 多路复用 |
| GUI 里点了按钮没反应 | 忙碌时点击被静默丢弃 | 见 §8 约束 22；现在会置灰按钮并提示 |
| GUI 旁边冒出一个黑色终端窗口 | 桥接子进程被分配了控制台 | 见 §8 约束 23；已改用 `CREATE_NO_WINDOW` |
| 双击 GUI 弹出一堆窗口、桥接却没起来 | GUI exe 用自己跑 `--serve` | 见 §8 约束 24；现在优先调用同目录 `cadbridge.exe` |
| 安装后敲 `cadbridge` 提示找不到 | PATH 改动对新终端才生效 | 重开一个命令行窗口 |
| 打包版所有子命令都打印帮助 | 冻结模式下 argv 起点没处理好 | 见 §8 约束 10；已修 |
| 输出中文乱码 | 按 ANSI 代码页编码了 | 见 §8 约束 11；已修 |
| `WSAEADDRINUSE(10048)` 连不上 | 临时端口被 TIME_WAIT 耗尽 | 见 §8 约束 13；已改成长连接复用 |
| 临时目录删不掉 `_MEIxxxx` | daemon 复用了父进程的解包目录 | 见 §8 约束 12；已修 |
| 中文变乱码（旧版） | argv 按 ANSI 解码 | 已内置修复；确认用最新版 |
| 桥接起不来 | AutoCAD 启动慢或被对话框挡住 | 跑 `cadbridge doctor`；看 `bridge.log` |

排查顺序建议：**先 `cadbridge doctor`**，它会逐项告诉你哪一环断了。
日志：`%LOCALAPPDATA%\CadBridge\bridge.log`；运行状态：`cadbridge status`。

## 12. 关闭与清理

```bash
cadbridge stop     # 只退桥接，AutoCAD 保留（推荐）
```

若需彻底清理进程：杀 `acad.exe` 与 `cadbridge.exe` 即可。
